app.git.login=${APP_GIT_LOGIN}
app.git.password=${APP_GIT_PASSWORD}
app.git.url=${APP_GIT_URL}

telegram.admin.id=${TELEGRAM_ADMIN_ID}

habr.telegram.channel_id=${HABR_TELEGRAM_CHANNEL_ID}
habr.telegram.bot.token=${HABR_BOT_TOKEN}

main_tech.telegram.bot.token=${MAIN_TECH_BOT_TOKEN}

spring.datasource.hikari.maximum-pool-size=2

habr.http.delay=5
habr.http.timeout=15

main_tech.alarm.url=${MAIN_TECH_ALARM_URL}

cron.alarm.send=${CRON_ALARM_SEND}
cron.alarm.clean=${CRON_ALARM_CLEAN}
cron.git.execute=${CRON_GIT_EXECUTE}
cron.habr.scan=${CRON_HABR_SCAN}
cron.dns.backup=${CRON_DNS_BACKUP}
cron.telegram.backup=${CRON_TELEGRAM_BACKUP}

cloudflare.dns.token=${CLOUDFLARE_DNS_TOKEN}
cloudflare.dns.url=${CLOUDFLARE_DNS_URL}
cloudflare.backuper.s3.url=${CLOUDFLARE_BACKUPER_S3_URL}
cloudflare.backuper.s3.region=${CLOUDFLARE_BACKUPER_S3_REGION}
cloudflare.backuper.s3.id=${CLOUDFLARE_BACKUPER_S3_ID}
cloudflare.backuper.s3.key=${CLOUDFLARE_BACKUPER_S3_KEY}
cloudflare.backuper.s3.bucket=${CLOUDFLARE_BACKUPER_S3_BUCKET}

telegram.backuper.tdl.url=${TELEGRAM_BACKUPER_TDL_URL}
telegram.backuper.s3.id=${TELEGRAM_BACKUPER_S3_ID}
telegram.backuper.s3.key=${TELEGRAM_BACKUPER_S3_KEY}
telegram.backuper.s3.bucket=${TELEGRAM_BACKUPER_S3_BUCKET}
telegram.backuper.filename=${TELEGRAM_BACKUPER_FILENAME}

habr.s3.id=${HABR_S3_ID}
habr.s3.key=${HABR_S3_KEY}
habr.s3.bucket=${HABR_S3_BUCKET}
